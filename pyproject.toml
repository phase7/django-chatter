[project]
name = "django-chatter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.6",
    "mysqlclient>=2.2.7",
    "python-decouple>=3.8",
]

[dependency-groups]
dev = ["pre-commit>=4.1.0"]
